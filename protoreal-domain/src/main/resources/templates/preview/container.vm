#foreach($item in $ContainerRenderer.getChildrenList())
	<div class="row">
  		#foreach($itemCell in $item.getChildrenList())
			<div class="col-sm" id="$itemCell.uuid">$itemCell.doPreview()<p></div>
		#end
	</div>	
#end
